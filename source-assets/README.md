Source assets are preserved here for future edits, but they are not served by Astro.

Keep only optimized, referenced launch assets in `public/`. Anything in `public/` is copied
directly into the production build.
